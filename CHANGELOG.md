# 变更记录

## 0.2.76 — 2026-09-18（本地测试包）

- SFTP 路径栏默认显示可点击的目录层级；点击根目录或上级目录直接跳转，沿用返回历史、目录树同步及旧目录大小计算取消逻辑。
- 点击地址栏空白处或编辑图标切换为完整路径输入，支持 Cmd/Ctrl+L、回车跳转；失焦或 Esc 放弃未提交内容并恢复面包屑，不额外请求服务器。
- 长路径折叠中间层级，可从省略菜单跳转；保留完整地址与目录名提示，适配亮暗主题。
- 本地 Apple Silicon 安装包要求 macOS 26.0 或更高版本，使用临时签名；覆盖升级后仍可能需要重新输入 SSH 密码。

## 0.2.75 — 2026-09-18（本地测试包）

- 修复首次加载文件时行号栏与文字区留白不同步的问题：不再屏蔽加载过程中的布局信号，行数、字体、主题和窗口尺寸变化时同步更新，避免行首被盖住、滚动后才恢复。
- 远端编辑器采用稳定等宽字体、轻量 YAML / JSON / INI 配色和当前行提示，统一亮暗主题下的标签、查找栏与底部状态；新增路径栏、保存 / 撤销 / 重做 / 查找 / 显示换行操作及行列位置。
- 高亮与显示换行不重写文本、不额外请求服务器；超过 512 Ki 字符的文件或 8192 字符的单行跳过高亮。新增首屏、行号位数变化、主题切换、窄窗口、保存撤销及大文件回归。
- 本地 Apple Silicon 安装包要求 macOS 26.0 或更高版本，显式使用临时签名；尚未完成正式 Developer ID 签名，不承诺覆盖升级后保留旧凭据的静默读取授权。

## 未发布 — macOS 升级凭据修复准备

- 定位到临时签名随构建变化导致旧 Keychain 条目无法静默读取；同构建重启测试不足以覆盖覆盖安装场景。本地自签证书不作为可靠修复方案。
- 正式打包要求明确的 Developer ID Application 证书指纹与固定 Apple Team ID，校验证书链和应用标识；缺失或不匹配时停止，禁止静默回退临时签名。开发测试必须显式选择临时签名。
- 新增两个不同构建、同构建重启、非受信构建拒绝的隔离 Keychain 回归；仅使用临时钥匙串及虚构密码，保持禁用授权框，不读取真实密码或放宽权限。
- 当前尚缺有效 Developer ID 证书，正式签名跨构建通过与新安装包仍待验证；不将这部分改动标为已经完成的用户端修复。

## 0.2.74 — 2026-09-18

- macOS 主窗口红色关闭按钮只隐藏窗口，保留 SSH 会话、终端内容与文件传输；点击 Dock 图标恢复原窗口，隐藏期间保持低频后台采样。
- 终端标签 ×、右键关闭当前 / 其他 / 全部会话统一增加确认；批量操作只询问一次，默认选中取消，支持 Esc 取消，确认前不清理连接或传输。
- Dock 右键退出、应用菜单退出及 Cmd+Q 统一确认真正退出；防止重复请求叠加弹框，保留未保存编辑器对退出的否决。取消后可继续使用原窗口与连接。
- 增加亮暗色、取消 / 确认、批量关闭、重入与标签变化回归，以及使用临时数据、模拟连接的 Cocoa 原生关闭 / Dock 恢复 / 退出流程验证。

## 0.2.73 — 2026-09-17

- 已完成的下载任务右侧新增“打开所在目录”文件夹按钮，使用系统文件管理器打开本地保存目录；上传、未完成任务及无效路径不显示入口。
- 仅响应用户主动点击，不打开下载文件、不执行命令；目录丢失或系统打开失败时在任务行提示，不额外弹窗，不增加 SSH 请求。
- 新增亮色 / 暗色、各传输状态、特殊字符路径、目标更新及目录丢失回归；测试拦截文件 URL，不启动 Finder 或读取真实下载文件。

## 0.2.72 — 2026-09-17

- 移除文件工具栏右侧排序下拉框，只保留文件名 / 大小表头排序；使用上下双箭头区分可排序列及当前方向，统一表头高度、留白和细分隔线，适配亮色与暗色。
- 目录大小的计算 / 重新计算改为大小单元格左侧的刷新图标，数值统一右对齐；等待与计算中显示低强调时钟图标，点击数值不会误触扫描。
- 保留默认关闭的自动计算、顺序队列与切换目录取消行为；图标按可见单元格绘制，不增加每行控件、动画定时器或远端请求。

## 0.2.71 — 2026-09-17

- 文件列表的目录大小列新增“计算 / 重新计算”，显示本次扫描的磁盘占用；右上角提供默认关闭的“自动计算”，按列表顺序单任务执行，切换目录、刷新、关闭开关或会话时取消旧任务，丢弃迟到结果。
- 统计通过独立 SSH exec 通道读取，不向交互终端输入命令；使用 nice / ionice（可用时）降低优先级，du 不跟随符号链接、不跨文件系统，跳过已识别的远程/虚拟文件系统；需要 du 和 timeout，单目录 60 秒限时，取消时通过控制通道停止远端子进程。扫描仍会产生目录元数据 I/O，不承诺零开销。
- 顶部及文件名/大小表头支持名称、大小升降序排序；大小按字节数比较，未计算或失败的目录排在末尾，不把部分结果当作完整大小。
- 新增路径引用、溢出校验、远端包装脚本取消、亮暗色界面、顺序队列/重算/导航取消和本地 SSH 通道并发回归；不连接真实服务器，不读取真实凭据。

## 0.2.70 — 2026-09-17

- 恢复密码框英文半角输入规则：全角字母、数字及常见中文/全角标点自动转换，汉字等不支持的字符整次拒绝写入并显示行内提示，避免混合粘贴被静默截断成另一串密码。
- SSH 密码、私钥口令、连接失败后的补填框及 RDP 密码使用同一规则；显示/隐藏密码、键盘输入、粘贴和输入法提交均覆盖，保留 ASCII 大小写及首尾半角空格。
- 仅处理本次界面输入，不扫描、转换或迁移已保存凭据，不改动钥匙串访问与 SSH 认证逻辑。新增输入法、光标/选区、剪贴板与本地回环 SSH 认证回归，保留原有 Unicode 凭据传输测试。

## 0.2.69 — 2026-09-15

- 未登录时，“访问历史 / 服务器管理”自动占满右侧可用高度，不再保留底部空白文件区和分隔条；已有会话返回起始页时同样使用完整高度。
- 切回终端时恢复文件区及用户调整的上下比例；手动隐藏文件区的设置不被导航覆盖，关闭最后一个标签后恢复全高起始页。
- 增加亮色/暗色下的全高布局、重复切换比例保持、手动隐藏状态和最后会话关闭回归，不改变 SSH 连接、凭据或服务器采样逻辑。

## 0.2.68 — 2026-09-15

- 将原左侧服务器列表并入右侧连接起始页，新增“访问历史 / 服务器管理”选项卡；移除 macOS 原生标题栏及其他平台工具栏中的服务器侧栏开关，保留监控栏开关。
- 服务器管理改为宽屏七列表格，显示名称、主机/IP、端口、用户、协议、认证方式和系统，保留分组、搜索、SSH/RDP 新建、双击连接、拖动归组与右键增删改；不显示密码内容。
- 终端“+”打开服务器管理，原终端标签仍可随时返回；导航不读取凭据、不重连、不清除已有终端或 SFTP 状态。
- 覆盖亮色/暗色、宽窄窗口、多列表格、历史连接与已有会话切换的界面回归；浏览使用本地配置，不增加服务器查询。

## 0.2.67 — 2026-09-14

- 修复修改已保存密码后，从旧终端标签右键重连仍读取旧凭据的问题；为已有会话保留独立的下一次连接配置，重连及复制会话使用更新后的引用，正在运行的连接不被改写或打断。
- 密码认证被服务器拒绝后，可直接在终端内重新输入并主动重试；明确显示实际用户名、IP 和端口，本次输入直达认证，不再读取旧密码。不自动反复连接，不因超时、主机指纹问题或不支持的认证方式而循环询问密码。
- 保持 v0.2.66 的无钥匙串授权框策略；失败/取消不记住新输入，认证成功才按用户选择保存。
- 新增旧标签重连复现、拒绝后重新输入、过期拒绝事件隔离，以及界面输入到真实 libssh2/回环 SSH 服务的端到端测试；固定虚拟密码包含中文、特殊符号及首尾空格。未读取真实密码或确认用户截图中的服务器已恢复登录。

## 0.2.66 — 2026-09-14

- 移除 v0.2.65 的“解锁凭据”按钮及显式授权代码路径；macOS Keychain 读取、保存、更新和删除都在本进程禁止交互的作用域内执行，失败直接返回，不请求 Mac 登录密码。
- 旧密码读不到时直接在终端内输入 SSH 密码或私钥口令；输入原样用于连接，不重复读取旧条目，取消后不会启动过期连接。SSH Agent 及未知主机指纹确认策略不变。
- 勾选记住密码后，仅在认证成功时保存为新的系统加密条目，原子更新对应主机的凭据引用；不覆盖旧条目，不覆盖连接期间发生的主机编辑。保存不可用不阻断本次 SSH 连接，不降级到明文存储。
- 手动修改密码使用新的凭据引用；仅修改主机元数据仍不访问密码。连接测试改为在原输入框提示填写服务器密码，RDP 提供远程密码编辑入口，不再提供系统授权入口。
- 增加重新输入、记住/不记住、失败/取消/旧结果、并发编辑等回归；原生 Keychain 用随机虚拟条目、独立限时进程检查读写删除及同一程序多次启动后的读取。临时签名仍可能在升级后改变身份，此时需要重新填写 SSH 密码，而非系统授权。

## 0.2.65 — 2026-09-14

- macOS 凭据默认静默读取，禁止未经主动点击的系统授权。移除 v0.2.64 的 `security` 子进程读取路径，避免系统工具绕过应用的静默控制再次弹框。
- 兼容旧式登录钥匙串：除了查询级禁止交互，还在本进程内串行、临时关闭该次读取的交互并立即恢复；不解锁钥匙串、不改 ACL、不把密码写入文件或命令行。
- SSH、连接测试和 RDP 在确需授权时展示界面内的“解锁凭据”按钮，仅点击按钮才允许系统询问；拒绝或取消不自动重试，取消中的旧授权结果不会重新启动连接。
- 编辑主机名称、地址等元数据且沿用原凭据时，不读取或重写密码；启动、浏览主机和最近登录列表不访问密码。
- 新增连续启动、重复连接、主动解锁/取消、连接测试和 RDP 的回归用例；真实钥匙串测试仅用随机虚拟条目，未授权读取在独立进程中设置 5 秒超时。仍为临时签名，不承诺未经授权即可读取受保护密码。

## 0.2.64 — 2026-09-11

- 修复 v0.2.62 将钥匙串读取改为原生 API 后，旧版凭据因授权读取程序不一致而反复要求输入 Mac 登录密码的兼容性回归；v0.2.63 的连接去重未解决这一根因。
- 读取前仅检查目标条目的授权元数据：已信任 `/usr/bin/security` 的旧条目沿用原读取程序，其他条目仍使用原生 API；读取失败不自动切换程序再次申请授权。
- 密码写入仍使用原生 Security 接口，不把密码放入进程参数、不修改现有 ACL、不解锁钥匙串、不迁移或删除现有凭据。钥匙串锁定或尚未授权的原生条目仍可能需要系统授权。
- 新增真实钥匙串虚拟旧条目回归：修复前稳定失败，修复后连续读取三次通过；同时验证原生条目的新增、读取、更新、删除。测试只使用随机虚拟条目。

## 0.2.63 — 2026-09-10

- 修复等待钥匙串授权及 SSH 建连期间重复双击同一主机再次读取凭据、重启连接的问题；授权阶段显示“正在读取系统凭据”，成功后继续原连接。
- 拒绝或读取凭据失败时将原因显示在终端，不再仅隐藏加载提示而留下空白；停止本次连接，允许用户主动重试。
- 取消或关闭标签后废弃尚未返回的授权结果；旧连接的延迟状态通知携带尝试编号，不得覆盖新连接状态。
- 未改动钥匙串访问权限、密码保存方式或签名身份；系统对不同条目或新版应用的首次授权仍可能出现。新增模拟授权重入、拒绝/重试、取消/关闭标签及真实 libssh2 状态编号回归。

## 0.2.62 — 2026-09-10

- 修复 macOS 暗色模式下 SSH/RDP 端口、字号及行间距等数字输入框仍显示白底的问题；统一输入区域、步进按钮、焦点和禁用样式，下拉分隔线与原生弹窗标题栏跟随主题，切换时保持控件尺寸稳定。
- 降低监控开销：优先用一次 awk 读取基础 /proc 指标，从 CPU 行推导核心数；进程列表每 5 秒、仅本地文件系统的磁盘占用每 30 秒查询，间隔内保留最近结果，不扫描 NFS/CIFS 挂载。
- 后台、最小化及隐藏监控区域时，基础采样降为每 5 秒；失败后按 2/4/8/16/30 秒退避，合并重复采样与同一路径的未完成目录请求。
- 终端流式输出时每 100 毫秒合并刷新搜索结果，导航时立即刷新；绘制仅二分定位当前可见区域的匹配，减少大量日志下的重复扫描。
- macOS 凭据读写改用原生 Security/Keychain 接口，不再通过进程参数传递编码后的密码；兼容原有服务名、账号及数据格式，不降低凭据库访问权限或主机指纹校验。
- 缩短会话内凭据副本的保留时间；断开时废弃过期采样与目录响应；指标输出限制为 512 KiB，防止异常服务端输出无限占用内存。
- 关闭会话时同步通知正在进行的传输取消，上传、下载及编辑读写循环响应连接取消，避免退出后仍持续处理文件内容；已在底层阻塞的单次操作仍受原有网络超时约束。
- 新增分频采样、部分指标保留、重复请求、搜索刷新、原生凭据库及真实 libssh2 采样后交互回归测试。仅在本机模拟环境测试，未对真实服务器压测，也不承诺零监控开销或解决网络不可达问题。

## 0.2.61 — 2026-09-09

- 修复 macOS 安装包内部 Qt、libssh2/OpenSSL 等依赖在部署改写后未重新签名，导致启动即被系统以 `CODESIGNING / Invalid Page` 终止的问题；部署完成后逐层签名并严格校验，失败将阻止打包。
- 增加最终应用包的原生 Cocoa 启动和 SQLite 插件检查，使用临时设置及数据库，不访问真实主机数据；临时签名不等于 Developer ID 签名或 Apple 公证。
- 修复终端输入在服务端停止接收时无限等待的问题；连续无进展达到 8 秒后断开，并支持及时取消，不自动重发可能已部分送达的命令。
- 终端持续输出改为按标准输出、错误输出分别限量读取并合并界面更新，减少按键、窗口尺寸调整和断开操作被长时间占用。
- 终端尺寸更新正确重试非阻塞发送；读取到连接错误时及时更新离线状态，普通通道 EOF 会先读完已缓存的输出。
- 启用 TCP_NODELAY，减少交互式小数据包等待；补齐无读写方向时的等待处理，避免空转。
- 增加 Ed25519、ECDSA、RSA-SHA256/SHA512 及多种加密组合、连续重连、重新协商密钥、发送堵塞、输出洪流和异常断线回归测试。测试使用本机模拟服务，不代表已完成各 Linux 发行版实机认证。
- 已知问题：服务端发送大量内容后立即关闭 TCP 时，libssh2 1.11.1 仍可能丢失末尾输出；保留明确的预期失败测试，本版未宣称修复该边界情况。

## 0.2.60 — 2026-09-09

- 修复认证方式查询超过 8 秒后仍继续认证、误报密码错误的问题；认证阶段独立等待最多 30 秒，并在超时或传输错误后停止，保留实际失败阶段。
- SSH 握手和认证改为可取消的非阻塞等待；取消或关闭连接时，不再等待整个认证超时，过时的连接请求不会重新启动。
- TCP 建连等待上限调整为 30 秒，明确区分端口不可达与认证失败，并改善多地址尝试及 Windows 超时错误提示。
- 密码和私钥口令按原样保留 Unicode、特殊标点及空格，不再自动替换或删除；修复新建连接输入内容可能被改写的问题。
- 显式输入的凭据优先使用；读取系统凭据库失败时明确提示，编辑留空沿用密码时不再将原凭据覆盖为空。
- 增加真实 libssh2 与本地模拟 SSH 服务的认证回归测试，以及新建主机保存、读取和连接请求的凭据回归测试。

## 0.2.59 — 2026-09-07

- 调整密码认证兼容策略：服务器同时开放普通密码与键盘交互认证时优先尝试普通密码，再回退到键盘交互，改善部分旧版 OpenSSH/PAM 服务器无法登录的问题。
- 键盘交互认证增加中文用户名提示识别，避免将密码错误填入“用户”“账号”或“账户”提示。
- 拆分过长的主题样式字符串，修复 Windows MSVC 编译时的 `C2026: string too big` 错误。
- 发布前增加 `main` 分支三平台构建验证；降低 Windows 构建并行度，并在失败时输出明确的编译错误。

## 0.2.58 — 2026-09-02

- 修复部分 Ubuntu 服务器未启用或不兼容 SFTP 时无法展示远程目录的问题；自动通过现有 SSH 会话进入目录兼容模式，并安全支持中文、空格及特殊字符路径。
- 同一连接确认需要兼容模式后，后续目录浏览将直接使用兼容读取，避免重复等待 SFTP 失败。

## 0.2.57 — 2026-09-01

- macOS DMG 新增“一键修复玄壳”入口；安装后可清除 `/Applications/玄壳.app` 的 quarantine 属性，并提供未安装、成功和权限失败提示。
- 新增 Windows 远程桌面（RDP）连接类型；可与 SSH 主机统一分组、搜索、复制、编辑和删除。
- Windows 使用系统 `mstsc`，macOS 生成标准 `.rdp` 文件并明确调用 Microsoft Windows App，Linux 可调用 FreeRDP。
- RDP 密码写入系统凭据库，不进入 SQLite、`.rdp` 文件或进程参数；macOS 连接时临时复制密码并在 60 秒后自动清除剪贴板。
- 新增跟随系统、亮色和暗色三种主题模式，并补齐文件目录树、编辑器、监控和弹层的暗色适配。
- 终端与远程文件编辑器的查找支持全文高亮、右侧位置标记及点击标记直接跳转。
- 终端命令输出块悬停时提供复制文本和复制图片入口；命令历史选择改为写入当前命令行而不立即执行。
- 优化 SSH 首屏输出和 SFTP 文件列表加载顺序，文件读取期间显示加载状态，减少连接后的空白等待。
- 监控摘要改为细进度条，CPU 悬停可按行查看各逻辑核心占用，并修复连续采样导致的布局变形。
- 调整 macOS 应用图标留白比例，优化编辑器标签、Esc 关闭弹层、vi 方向键及多处界面细节。

## 0.2.52 — 2026-08-24

- 分组改为完全可选：可在主机编辑窗口明确清除分组，未设置分组的主机直接平铺，左侧不再生成“未分组”伪目录。
- 终端显示设置中的字体控件改为带明确箭头的系统字体下拉框，可直接选择本机已安装字体，不再呈现为可自由输入的文本框。
- 命令记录的“清空历史”仅删除未收藏记录，收藏命令及备注保持不变；“清空收藏”独立取消收藏标记，命令仍保留在历史中。

## 0.2.51 — 2026-08-21

- Windows 改用与 macOS 相同的单行标题栏布局：主机栏、监控栏和终端设置三个工具图标与最小化、最大化、关闭按钮处于同一行，并保留拖动、双击最大化和窗口边缘缩放。
- 主机编辑窗口将 IP/域名与端口合并到一行；分组改为带明确箭头的只选下拉框，固定提供“未分组”并加载全部已保存分组。
- 分组右键菜单新增“新建连接”，打开连接窗口时自动预选当前分组。
- 主机列表默认收起；终端标签栏“+”打开最近登录起始页时自动展开，从最近登录记录打开会话后再次收起；实时监控栏顶部地址精简为仅显示 IP。
- 移除文件工具栏中与右键菜单重复的“…”按钮；上传或下载任务加入队列时自动展开传输详情，空闲时保持界面简洁。
- 文件区返回、上级、刷新与传输队列统一为 26×26 的紧凑正方形图标按钮，使用一致的线条图标、圆角和悬停反馈。
- 修正文件路径输入框被全局最小高度撑开的问题，工具栏增加稳定的上下内边距并保持输入框垂直居中。
- 左侧主机列表升级为可展开的分组树：支持分组新建、重命名和删除，主机可拖拽或通过右键菜单移动分组，空分组也会持久保存。
- 文件列表右键菜单新增权限管理，可分别设置所有者、组和其他用户的读/写/执行权限；目录支持递归应用到全部项目、仅文件或仅目录。
- 文件列表支持多选后批量删除；一次确认后逐项执行，单个项目失败不会中断其余项目，并在完成后统一刷新结果。
- 终端 Tab 按键按物理按键周期去重并过滤自动重复，避免单次补全被远端 Bash 误判为连续 Tab 后直接询问是否展开大量候选。
- 窗口右上角新增终端显示设置；可统一调整 SSH 黑色终端区域的等宽字体、字号和行间距，支持实时预览、恢复默认并持久化到下次启动。
- 终端会话标签保持完整栏宽，并通过 Qt 原生样式提示从左侧排列；避免压缩标签栏导致 macOS 停止绘制标签。
- 移除终端右上角独立“清屏”按钮，将清屏操作合并到标签右键菜单，并严格作用于被右键点击的会话。
- 标签关闭按钮固定在右侧，名称宽度随内容收缩且最多显示 10 个字符；标签组右侧新增“+”入口，可在保留现有 SSH 会话的同时打开最近登录起始页。
- 调整标签关闭按钮左右间距，使右侧留白与左侧连接状态标识保持视觉对称，不再紧贴标签边界。
- 终端正文区域增加小幅内边距，避免首行输出和左右边界、滚动条贴合过紧。
- 实时监控默认合并为四项单行摘要；标题栏“详细”入口一次展开全部趋势、阈值与告警信息，减少常驻侧栏占用。
- 监控摘要的彩色占用指示改为横向细进度条，并固定摘要容器高度，避免窄侧栏中出现竖线或行距被拉伸。
- 终端输入栏右侧新增命令记录入口；管理面板向上展开，并按当前主机提供历史记录与收藏两个视图，支持再次执行、收藏、备注、逐条删除和清空。
- 相同命令只保留一条记录并更新最近执行时间；收藏与备注持久化到 SQLite，重新启动应用后仍可继续使用。
- 终端输入栏新增文件区显隐按钮，可快速收起或恢复底部 SFTP 文件管理；按钮状态在所有终端标签间保持同步。
- 将终端正文边距落实到自绘坐标系，文字、光标、选择区和鼠标报告统一从左 14px、上 10px 的内容原点开始，避免首行输出紧贴顶部和左侧边框；移除与标签状态、顶部在线标识重复的终端绿色连接状态栏，为正文释放一行高度。
- 重做传输队列弹层，修复上传后面板高度塌缩、任务详情不可见；弹层现在直接展示文件名、状态、进度、已传/总大小、目标路径和取消/重试操作，并提供清晰的空队列提示。

## 0.2.50 — 2026-08-18

- 终端鼠标交互测试改用 `TerminalView` 自身的字符网格尺寸计算选择和鼠标报告坐标，消除 Windows 与 macOS 系统字体度量不同造成的断言误报。
- Windows 本地失败连接测试的异步等待上限提高到 10 秒，覆盖 GitHub runner 上约 6.5 秒的网络失败回报时间，同时保持事件循环响应性检查。
- Windows 工作流显式区分构建失败与无界面诊断测试失败，并在记录后清除后者的进程退出码，确保 NSIS EXE 和便携 ZIP 打包步骤继续执行。

## 0.2.49 — 2026-08-18

- macOS 打包脚本不再假设 Homebrew 的 `share/qt/plugins` 目录结构，改由 `qtpaths`/`qmake` 查询当前 Qt 安装的真实插件目录，兼容 GitHub Actions 的 Qt 安装布局。
- Windows 发布继续执行完整无界面 UI 冒烟测试并输出详细日志；CI 图形环境特有的 UI 用例失败改为警告，不再阻断已经成功编译的 NSIS EXE 与便携 ZIP 生成，编译和打包错误仍会阻断发布。

## 0.2.48 — 2026-08-17

- 全平台 UI 冒烟测试统一使用 Qt `offscreen` 平台插件；编辑主机快照测试改为隐藏工作区验证，避免 macOS Qt 6.8 `minimal` 插件显示复杂窗口时发生段错误。
- Windows GitHub Actions 改为直接运行测试程序，将详细 QTest 输出写入日志文件后打印；测试失败时可准确看到失败函数，同时不再受 CTest 捕获 Windows GUI 输出的限制。

## 0.2.47 — 2026-08-17

- 终端标签菜单测试改为在隐藏工作区中直接准备上下文并触发动作，不再依赖 Qt 6.8 `minimal` 平台插件的窗口布局、坐标命中和原生菜单生命周期。
- Windows 测试程序明确使用控制台子系统和更完整的 `offscreen` 平台插件，并启用详细 QTest 输出，避免 `minimal` 布局误报，同时确保 GitHub Actions 能显示具体测试函数和失败断言。

## 0.2.46 — 2026-08-17

- 修复 macOS GitHub Actions 在 `minimal`/`offscreen` 无界面平台插件下显示终端标签右键菜单时发生段错误的问题；菜单动作状态与行为仍由完整测试覆盖。
- Windows GitHub Actions 改用 Visual Studio 2022 x64/MSVC 生成器，与 MSVC 版 Qt 和 `x64-windows-static-md` 依赖保持同一工具链，消除 MinGW 链接 MSVC 静态库时的安全 Cookie 符号错误。

## 0.2.45 — 2026-08-17

- 修复 Qt 6.8 不支持 `qt_generate_deploy_app_script(NO_PLUGINS)` 导致两种 macOS GitHub Actions 构建在 CMake 配置阶段失败的问题；macOS DMG 统一由发布脚本部署所需插件和运行库。
- 将文件操作枚举从 `CreateDirectory` 改为 `MakeDirectory`，避免 Windows SDK 将其宏展开为 `CreateDirectoryW` 并造成编译失败。
- 自动发布聚焦 macOS Apple Silicon、macOS Intel 与 Windows x64，移除会阻塞 Release 的 Linux 安装包任务和下载入口。

## 0.2.44 — 2026-08-15

- macOS 发布产物改为单文件、LZMA 压缩的 DMG，内含“玄壳.app”和 Applications 拖拽入口；ZIP 改为可选备用产物。
- 明确设置并校验 macOS 14.0 最低部署版本，修复主程序被当前 SDK 错误标记为仅支持 macOS 26 的问题。
- 精简未使用的 Qt TLS 与 SVG 图片格式插件，减少应用和安装包体积。
- 增加 Info.plist 与 Mach-O 最低系统版本一致性检查，避免后续打包再次产生错误兼容提示。

## 0.2.43 — 2026-08-15

- 产品中文名称完整迁移为“玄壳”，英文名称统一为 `NoxShell`；窗口、日志、演示内容、开发文档与界面文案同步更新。
- CMake 项目、可执行文件、macOS `.app`、测试目标、图标资源、发布包和内部 C++ 命名空间全部改用 NoxShell 名称。
- 数据库、日志、断点续传文件、环境变量、凭据服务与临时构建目录统一使用 `noxshell` 前缀。
- 清除旧品牌数据库路径、凭据服务名和兼容迁移代码，源码与运行产物仅使用玄壳/NoxShell 命名。
- 新增应用中文名、英文组织名与版本标识回归测试。

## 0.2.42 — 2026-08-15

- 使用用户提供的透明裁切版黑金终端图标，去除外围黑色留边并放大有效图标主体。
- 重新生成全部 macOS Retina `.icns` 尺寸，Dock、Finder、窗口和发布包同步更新。
- 图标资源测试新增 Alpha 通道与透明角像素校验，防止外围黑边再次出现。

## 0.2.41 — 2026-08-15

- 使用用户提供的黑金终端图替换原蓝色应用图标，窗口、Dock、Finder 和发布包统一使用新视觉。
- 新增完整的 macOS Retina `.icns` 资源，覆盖 16、32、64、128、256、512 和 1024px 尺寸，并在应用 `Info.plist` 中正式声明。
- 增加可重复执行的 macOS 图标生成脚本，统一输出带 Alpha 通道的 Retina 图标集，并增加资源加载回归测试，避免后续打包恢复旧图标。

## 0.2.40 — 2026-08-15

- 修复通过底部命令输入行启动 `top` 等交互程序后，`Ctrl+C` / `Cmd+C` 被输入框复制行为吞掉、无法退出的问题。
- 提交命令后自动把焦点交给原始终端；无文本选区时 `Ctrl+C` 与 `Cmd+C` 均发送远端中断字符，有选区时仍执行复制。
- 增加终端输出区、命令输入行以及 macOS Command 键的中断回归测试。

## 0.2.39 — 2026-08-15

- 最近登录记录改为按主机去重，同一主机重复连接只更新时间，不再追加重复行。
- 数据库升级时自动合并已有重复历史，仅保留每台主机时间最新的一条记录。
- 增加重复写入、乱序时间写入和旧数据库迁移回归测试，确保最近登录时间不会倒退。

## 0.2.38 — 2026-08-15

- 修复终端使用物理 `Ctrl+V` 时被发送为 Shell 控制字符并显示 `^V`、无法粘贴剪贴板内容的问题。
- `Ctrl+V`、`Cmd+V` 和 `Ctrl+Shift+V` 现在统一走终端粘贴通道；括号粘贴模式继续生效。
- 右键复制或粘贴菜单关闭后自动将键盘焦点还给终端，可立即继续输入或使用快捷键粘贴。
- 增加 `Ctrl+V` 粘贴负载回归测试，确保不再发送 `0x16` 控制字符。

## 0.2.37 — 2026-08-15

- 终端输出区新增右键菜单，选中文本后可右键复制，在终端光标处可右键粘贴，并提供全选操作。
- 支持 `Cmd+C/Cmd+V` 直接复制粘贴及 `Ctrl+Shift+C/Ctrl+Shift+V` 终端快捷键；有选区时 `Ctrl+C` 执行复制，无选区时仍发送远端中断信号。
- 粘贴统一经过 SSH 原始输入和括号粘贴通道，并同步清除旧选区、维护命令跟踪状态。
- 右键保留为本地菜单，即使远端程序启用鼠标报告也不会收到误操作；增加复制、粘贴、快捷键和鼠标报告共存回归测试。

## 0.2.36 — 2026-08-15

- 查找栏新增始终可见的“替换”入口，点击展开替换输入框、“替换”和“全部替换”，解决只能发现查找而找不到替换的问题。
- 展开后按钮切换为“收起替换”，再次点击可恢复紧凑单行查找栏；原有 `Cmd+Option+F` / `Ctrl+H` 快捷键继续保留。
- 增加替换入口可见性、展开/收起状态和快捷键状态同步回归测试。

## 0.2.35 — 2026-08-15

- 修复 macOS Cocoa 原生样式下终端会话标签栏高度塌缩，已有标签内容无法显示、顶部只剩空白工具条的问题。
- 为会话标签栏设置稳定的 32px 布局高度，单标签、多标签及连接状态图标均保持可见。
- 增加 Cocoa 原生平台的标签可见性、控件高度、标签宽度、当前项和标题回归测试。

## 0.2.34 — 2026-08-15

- 远端文件编辑器新增紧凑型查找/替换面板，不操作时保持隐藏，不占用文件内容区域。
- 支持 `Cmd/Ctrl+F` 查找、macOS `Cmd+Option+F` 或 `Ctrl+H` 展开替换、Enter/Shift+Enter 循环定位和 Esc 收起。
- 新增匹配位置/总数、区分大小写、替换当前项和全部替换；替换操作进入编辑器撤销栈并同步标记文件未保存状态。
- 增加查找、循环定位、替换和脏状态回归测试。

## 0.2.33 — 2026-08-15

- 修复编辑主机 IP/端口后仍携带旧地址指纹，导致新服务器被“指纹不匹配”阻断的问题。
- 编辑窗口检测到地址或端口变更时自动清除原目标指纹；新地址首次连接会按安全流程重新确认，旧地址指纹记录不受影响。
- 终端标签新增连接状态图标：灰色空心点表示未连接，蓝色进度环表示连接中，绿色实心点表示连接成功；悬停提示展示状态、地址和底层连接信息。
- 终端标签右键菜单新增“连接”和“断开连接”，根据当前标签状态自动启用可执行的操作。
- 增加端点变更指纹清理、标签状态及右键连接/断开的回归测试。

## 0.2.32 — 2026-08-15

- 左侧第一栏收敛为纯主机入口，移除每台主机的在线/离线圆点与状态提示。
- 单击主机只保留列表选中项，不再激活或切换终端标签；终端、监控和 SFTP 联动统一由上方终端标签控制。
- 双击主机建立连接后自动隐藏第一栏，为终端和文件区释放宽度；可从 macOS 标题栏图标随时恢复。
- 第一栏编辑或删除其他主机时不再改变、断开当前终端快照，并修复连续删除主机后删除状态未释放的问题。
- 更新主机列表和主窗口回归测试，覆盖无状态标记、单击不切标签与双击自动收起。

## 0.2.31 — 2026-08-15

- 主机配置修改改为会话快照模型：已连接终端继续使用建立 SSH 时的原主机名、IP、端口和凭据。
- 保存主机修改不再断开、清屏或自动重连旧会话；下一次双击或右键连接时创建使用新配置的独立标签。
- 同一主机的新旧配置标签可并存，后续连接优先激活最新配置会话，避免重复建立 SSH。
- 增加在线编辑主机的回归测试，验证旧通道不重连、旧标签不改写以及新连接使用新 IP/端口。

## 0.2.30 — 2026-08-15

- 启动时固定保持未连接状态，不再恢复上次打开但未连接的终端标签；单击左侧主机也不会创建空标签。
- 当前没有 SSH 会话时隐藏整个标签栏与空终端内容，右上区域改为“最近登录”页面。
- SQLite 新增成功登录历史表，只在 SSH 认证成功后记录真实登录时间，并按登录时间倒序展示主机、地址、用户和时间。
- 最近登录记录支持双击重新连接；最后一个终端标签关闭后自动返回记录页。
- 增加旧标签状态不恢复、登录历史倒序、服务器删除级联清理及空闲/会话页面切换回归测试。

## 0.2.29 — 2026-08-15

- 修正 v0.2.28 的顶部控制区仍以独立 Qt 工具栏占据一行的问题。
- macOS 改用 Cocoa `NSTitlebarAccessoryViewController`，将主机列表与实时监控栏开关直接嵌入红黄绿系统按钮右侧。
- Cocoa 环境不再创建 `QToolBar`，应用内容紧贴系统标题栏，进一步释放顶部可视高度。
- 非 Cocoa 和无界面测试环境保留兼容工具栏，两个开关的行为与状态提示保持一致。

## 0.2.28 — 2026-08-15

- 删除整条深蓝色自定义顶栏及其中的品牌块、全局搜索占位、连接服务和用户信息，释放 56px 工作区高度。
- 主机列表开关移动到窗口顶部工具栏；macOS 下启用标题栏与工具栏融合。
- 顶部新增实时监控栏开关，可独立隐藏或恢复左侧第二栏，终端与文件区自动填充释放出的宽度。
- 为两个开关提供不同的栏位图标、展开/收起状态、悬停说明和无障碍名称，并增加双栏切换回归测试。

## 0.2.27 — 2026-08-14

- 文件管理的返回、上级、路径输入和刷新控件整体上移到标题工具栏，删除原有的独立路径栏。
- 移除标题栏中的“上传”“下载”和“+目录”按钮，减少常驻按钮和误操作；上传继续支持 Finder 拖拽，下载保留在右键菜单。
- 文件列表右键菜单新增“新建目录”，并与“新建文件”、下载、重命名和删除集中在同一操作菜单中。
- 增加单行工具栏布局和菜单入口回归测试，确保旧按钮不再生成。

## 0.2.26 — 2026-08-14

- 移除文件管理区底部常驻的传输队列和状态栏，释放更多目录浏览高度。
- 当前目录/传输状态合并到文件管理顶部工具栏，并在上传、下载等操作时原位更新。
- 工具栏新增传输队列图标；点击后以下拉面板展示任务详情、限速、展开/收起、取消与失败重试。
- 有正在执行或等待的传输任务时队列图标高亮，悬停提示同步展示活动状态和任务总数。

## 0.2.25 — 2026-08-14

- 修复旧版 SFTP v3 服务端在覆盖保存已有文件时返回 `SFTP 4`，导致临时文件无法提交的问题。
- 保存时优先使用 OpenSSH `posix-rename` 原子替换；不支持扩展时再尝试标准覆盖重命名。
- 对既不支持 POSIX rename、也拒绝覆盖标志的旧服务器增加备份提交回退，提交失败时自动恢复原文件。
- 上传文件复用同一套兼容提交逻辑，避免覆盖远端同名文件时出现相同错误。

## 0.2.24 — 2026-08-14

- 修复 macOS 下标准保存快捷键与额外 `Ctrl+S` 映射成同一按键序列，产生快捷键歧义而无法保存的问题。
- 编辑器改为直接处理按键事件，macOS 的 `⌘S` 和物理 `Ctrl+S` 均可保存，`⌘Z/Ctrl+Z`、`⌘W/Ctrl+W` 同样兼容。
- 回归测试真实发送 `⌘S` 与 `Ctrl+S`，分别保存后通过 SFTP 重新读取内容确认写入成功。

## 0.2.23 — 2026-08-14

- 远端文件编辑器改为上方文件标签、下方代码编辑区的多标签窗口；同一服务器打开多个文件时复用窗口。
- 文件标签显示“服务器名称 · 文件名”，未保存的标签使用红色圆点提示，保存成功后自动消失。
- 移除编辑器中的“关闭”和“保存”按钮，改用 `Cmd/Ctrl+S` 保存、`Cmd/Ctrl+Z` 撤销、`Cmd/Ctrl+W` 关闭当前标签。
- 编辑区新增行号、当前行高亮、等宽字体与 4 空格制表位；`Ctrl+/` 可对当前行或选中多行切换 `#` 注释。
- 关闭文件标签或整个编辑窗口时会检查所有未保存内容，并提供保存、放弃或取消关闭选项。

## 0.2.22 — 2026-08-14

- 左侧主机项改为“名称 / IP”单行布局，在线状态圆点固定显示在最右侧，并缩短列表项高度。
- 终端标签新增右键菜单，支持复制会话、关闭当前、关闭其他和关闭全部。
- 移除标签栏右侧独立“复制会话”按钮，会话操作统一收拢到所点标签的右键菜单；清屏按钮继续保留。
- 增加标签右键菜单的复制与三种关闭方式回归测试，校验“关闭其他”会正确保留右键所点会话。

## 0.2.21 — 2026-08-14

- 双击 SFTP 普通文件会打开独立的非模态编辑窗口，读取和保存复用当前终端的 SSH 会话。
- 编辑器支持 `Cmd/Ctrl+S`、未保存关闭确认、二进制内容拦截和 4 MiB 大小限制；保存使用远端临时文件原子替换并保留原文件权限。
- 文件区右键菜单和“⋯”菜单新增“新建文件”，创建空文件后自动刷新当前目录，同名目标存在时拒绝覆盖。
- 增加远端文件创建、读取、覆盖保护、编辑保存和目录刷新回归测试。

## 0.2.20 — 2026-08-14

- 左侧主机栏移除“主机 + 数量”标题行，新增按钮移到搜索框右侧并与其同行。
- 主机列表改为“名称 / 服务器地址”两行紧凑布局，右上角用绿色圆点表示在线，灰色空心点表示离线。
- 关闭某主机的最后一个终端标签后，左侧该主机立即更新为离线；若还有同主机在线标签则保持在线。
- SSH 终端标签文字精简为主机名称，不再显示 `root@`等用户名前缀。

## 0.2.19 — 2026-08-14

- 监控与 SFTP 改为直接复用每个终端标签已建立的 SSH 会话，移除切换主机时断开、重连辅助会话的机制。
- 多个终端标签保持各自在线，标签或左侧主机切换只切换监控与文件界面的展示对象，不再重新握手和认证。
- 每个 SSH 会话保留独立的 SFTP 界面状态，切换返回后恢复原来的目录、目录树和文件列表。
- 回归测试新增“双主机在线往返切换时零重连”与 SFTP 目录保留校验。

## 0.2.18 — 2026-08-14

- 修复从左侧切换到已在线终端时，只更新选中主机却未重建监控/SFTP 辅助 SSH 会话的问题。
- 终端认证成功后会再次校验当前主机的辅助会话，避免终端在线而监控显示“采样失败”、SFTP 显示“SSH 会话未连接”。
- 增加两台在线主机从左侧往返切换的回归测试，同时校验监控采样与远程文件列表恢复。

## 0.2.17 — 2026-08-14

- 切换 SSH 终端标签时同步选中左侧对应主机，并刷新主机地址、在线状态、监控历史与 SFTP 文件区。
- 切换到已在线终端时自动建立对应主机的监控/SFTP辅助会话；未连接的恢复标签仍保持手动连接策略。
- 左侧主机选择与终端标签切换改为双向联动，避免终端和监控/文件区显示不同服务器。

## 0.2.16 — 2026-08-14

- 密码与私钥口令框增加英文半角输入提示，关闭中文联想、自动大写和预测输入。
- 自动将中文句号 `。`、全角标点及全角字母数字转换为对应英文半角字符。
- 无法转换的中文/非 ASCII 字符会被阻止，并在密码框下方提醒切换英文输入法和显示核对。

## 0.2.15 — 2026-08-14

- 编辑主机时明确说明空密码框会沿用 Keychain 已保存密码，连接测试状态同步显示本次凭据来源。
- 密码框新增显示/隐藏操作，便于核对包含中文或特殊字符的当前输入。
- 已保存密码认证失败时给出重新输入或切换私钥认证的针对性提示，减少将旧凭据误判为 SSH 会话冲突。

## 0.2.14 — 2026-08-14

- 密码认证增加 `keyboard-interactive` 自动协商与 `password` 回退，兼容由 PAM/面板配置为键盘交互密码的 SSH 服务端。
- 键盘交互凭据采用线程局部上下文，多 SSH 会话并发认证时不会互相覆盖。
- 认证失败信息新增服务端允许方式与客户端已尝试方式，便于区分凭据错误和认证方式不匹配。

## 0.2.13 — 2026-08-14

- 终端“清屏”改为标准 `Ctrl+L` 语义：清除本地滚屏历史后通知远端 Shell 重绘，保留一行可继续输入的提示符。
- 演示 SSH 会话同步支持 `Ctrl+L` 屏幕重绘，与真实 Bash 会话表现一致。

## 0.2.12 — 2026-08-14

- SFTP 文件管理改为双栏布局：左侧目录树按节点逐层异步加载，右侧保留当前目录的完整文件明细。
- 目录树与路径栏、返回/上级、终端 `cd` 命令联动，并自动展开和选中当前路径。
- 右侧文件列表支持 Shift/Cmd 多选，右键可下载所选文件；多文件下载时统一选择本地目标目录。
- 支持从 Finder 拖拽一个或多个本地文件到右侧区域上传，并可将文件直接投放到某个远程目录。

## 0.2.11 — 2026-08-14

- 删除终端上方的工作区说明栏和终端内部重复标题栏，“清屏”移到“复制会话”右侧。
- 监控栏主机标识精简为 `IP:端口`，新增一键复制 IP 图标。
- 在线状态改为聚合监控/SFTP 和多终端实际连接结果，修复 SSH 已通但仍显示离线的问题。
- 双击连接后终端显示连接中 Loading 提示板，并随握手、指纹校验和认证阶段更新说明。

## 0.2.10 — 2026-08-14

- 移除工作区右上角的刷新、测试连接、编辑、删除和新建会话按钮，避免对当前主机的误操作。
- 新增/编辑 SSH 主机窗口新增“连接测试”，使用当前未保存的表单配置验证 TCP、SSH 握手、主机指纹和认证。
- 编辑时密码或私钥口令留空，连接测试会安全沿用系统凭据库中的原凭据；新输入值不会被旧凭据覆盖。

## 0.2.9 — 2026-08-14

- 主界面改为三块同时可见的运维布局：左侧纵向监控栏、右上 SSH 终端、右下文件管理。
- 移除监控/终端/文件管理之间的主页面切换，连接后可同时观察指标、操作终端和浏览文件。
- 新增横向与纵向可拖动分隔条；监控趋势改为按需展开，在紧凑侧栏中保留历史趋势、阈值和告警能力。

## 0.2.8 — 2026-08-14

- 顶部品牌名称右侧新增侧栏图标按钮，可收起和恢复左侧主机列表。
- 侧栏隐藏后主工作区自动扩展；按钮图标、悬浮提示和无障碍名称会随状态切换。

## 0.2.7 — 2026-08-14

- 主机选择与连接解耦：启动和单击左侧主机不再自动连接，双击或右键“连接”才启动 SSH、监控和 SFTP。
- 左侧主机增加右键菜单，支持连接、编辑、复制主机配置、复制连接地址和删除。
- 终端提交 `cd` 命令后同步右侧 SFTP 目录，支持绝对路径、相对路径、`..`、`~`、`$HOME` 及 Tab 补全后的命令识别。
- 恢复的终端标签默认处于待连接状态，独立“测试连接”成功后不再隐式替换现有终端连接。

## 0.2.2 — 2026-08-13

- 修复测试连接成功或修改凭据后，已打开终端标签仍保留旧认证失败状态的问题。
- SFTP 初始目录改为通过 `realpath(".")` 自动定位远端用户主目录，不再固定假设 `/var/www/app` 存在。
- 远端主目录无法解析时自动回退到根目录，并展示明确状态。

## 0.2.1 — 2026-08-13

- 修复 Qt TCP 缓冲区提前读取服务端 SSH banner，导致 libssh2 握手报 `Failed sending banner (-43)` 的问题。
- SSH 传输层改为 libssh2 独占的原生 IPv4/IPv6 socket，补充跨平台连接超时和 socket 错误信息。
- 增加可选真实服务器握手回归测试；不会提交密码或执行远端命令。

## 0.2.0 — 2026-08-13

- SSH 终端升级为真实多标签、独立连接、会话复制和启动恢复。
- 监控历史落盘保存 7 天，支持每台服务器独立阈值与告警事件记录。
- 传输队列支持持久化、异常退出恢复、失败重试、断点续传和会话级限速。
- 凭据存储覆盖 macOS Keychain、Windows Credential Manager 与 Linux Secret Service。
- 新增滚动日志、敏感字段脱敏、应用图标和跨平台发布脚本。

## 0.1.0

- 完成 SSH 会话、VT/ANSI 终端、Linux 指标采集与 SFTP 文件管理基础能力。
