# 变更记录

## 0.2.46 — 2026-08-17

- 修复 macOS GitHub Actions 在 `minimal`/`offscreen` 无界面平台插件下显示终端标签右键菜单时发生段错误的问题；菜单动作状态与行为仍由完整测试覆盖。
- Windows GitHub Actions 改用 Visual Studio 2022 x64/MSVC 生成器，与 MSVC 版 Qt 和 `x64-windows-static-md` 依赖保持同一工具链，消除 MinGW 链接 MSVC 静态库时的安全 Cookie 符号错误。

## 0.2.45 — 2026-08-17

- 修复 Qt 6.8 不支持 `qt_generate_deploy_app_script(NO_PLUGINS)` 导致两种 macOS GitHub Actions 构建在 CMake 配置阶段失败的问题；macOS DMG 统一由发布脚本部署所需插件和运行库。
- 将文件操作枚举从 `CreateDirectory` 改为 `MakeDirectory`，避免 Windows SDK 将其宏展开为 `CreateDirectoryW` 并造成编译失败。
- 自动发布聚焦 macOS Apple Silicon、macOS Intel 与 Windows x64，移除会阻塞 Release 的 Linux 安装包任务和下载入口。

## 0.2.44 — 2026-08-15

- macOS 发布产物改为单文件、LZMA 压缩的 DMG，内含“玄壳.app”和 Applications 拖拽入口；ZIP 改为可选备用产物。
- 明确设置并校验 macOS 14.0 最低部署版本，修复主程序被当前 SDK 错误标记为仅支持 macOS 26 的问题。
- 精简未使用的 Qt TLS 与 SVG 图片格式插件，减少应用和安装包体积。
- 增加 Info.plist 与 Mach-O 最低系统版本一致性检查，避免后续打包再次产生错误兼容提示。

## 0.2.43 — 2026-08-15

- 产品中文名称完整迁移为“玄壳”，英文名称统一为 `NoxShell`；窗口、日志、演示内容、开发文档与界面文案同步更新。
- CMake 项目、可执行文件、macOS `.app`、测试目标、图标资源、发布包和内部 C++ 命名空间全部改用 NoxShell 名称。
- 数据库、日志、断点续传文件、环境变量、凭据服务与临时构建目录统一使用 `noxshell` 前缀。
- 清除旧品牌数据库路径、凭据服务名和兼容迁移代码，源码与运行产物仅使用玄壳/NoxShell 命名。
- 新增应用中文名、英文组织名与版本标识回归测试。

## 0.2.42 — 2026-08-15

- 使用用户提供的透明裁切版黑金终端图标，去除外围黑色留边并放大有效图标主体。
- 重新生成全部 macOS Retina `.icns` 尺寸，Dock、Finder、窗口和发布包同步更新。
- 图标资源测试新增 Alpha 通道与透明角像素校验，防止外围黑边再次出现。

## 0.2.41 — 2026-08-15

- 使用用户提供的黑金终端图替换原蓝色应用图标，窗口、Dock、Finder 和发布包统一使用新视觉。
- 新增完整的 macOS Retina `.icns` 资源，覆盖 16、32、64、128、256、512 和 1024px 尺寸，并在应用 `Info.plist` 中正式声明。
- 增加可重复执行的 macOS 图标生成脚本，统一输出带 Alpha 通道的 Retina 图标集，并增加资源加载回归测试，避免后续打包恢复旧图标。

## 0.2.40 — 2026-08-15

- 修复通过底部命令输入行启动 `top` 等交互程序后，`Ctrl+C` / `Cmd+C` 被输入框复制行为吞掉、无法退出的问题。
- 提交命令后自动把焦点交给原始终端；无文本选区时 `Ctrl+C` 与 `Cmd+C` 均发送远端中断字符，有选区时仍执行复制。
- 增加终端输出区、命令输入行以及 macOS Command 键的中断回归测试。

## 0.2.39 — 2026-08-15

- 最近登录记录改为按主机去重，同一主机重复连接只更新时间，不再追加重复行。
- 数据库升级时自动合并已有重复历史，仅保留每台主机时间最新的一条记录。
- 增加重复写入、乱序时间写入和旧数据库迁移回归测试，确保最近登录时间不会倒退。

## 0.2.38 — 2026-08-15

- 修复终端使用物理 `Ctrl+V` 时被发送为 Shell 控制字符并显示 `^V`、无法粘贴剪贴板内容的问题。
- `Ctrl+V`、`Cmd+V` 和 `Ctrl+Shift+V` 现在统一走终端粘贴通道；括号粘贴模式继续生效。
- 右键复制或粘贴菜单关闭后自动将键盘焦点还给终端，可立即继续输入或使用快捷键粘贴。
- 增加 `Ctrl+V` 粘贴负载回归测试，确保不再发送 `0x16` 控制字符。

## 0.2.37 — 2026-08-15

- 终端输出区新增右键菜单，选中文本后可右键复制，在终端光标处可右键粘贴，并提供全选操作。
- 支持 `Cmd+C/Cmd+V` 直接复制粘贴及 `Ctrl+Shift+C/Ctrl+Shift+V` 终端快捷键；有选区时 `Ctrl+C` 执行复制，无选区时仍发送远端中断信号。
- 粘贴统一经过 SSH 原始输入和括号粘贴通道，并同步清除旧选区、维护命令跟踪状态。
- 右键保留为本地菜单，即使远端程序启用鼠标报告也不会收到误操作；增加复制、粘贴、快捷键和鼠标报告共存回归测试。

## 0.2.36 — 2026-08-15

- 查找栏新增始终可见的“替换”入口，点击展开替换输入框、“替换”和“全部替换”，解决只能发现查找而找不到替换的问题。
- 展开后按钮切换为“收起替换”，再次点击可恢复紧凑单行查找栏；原有 `Cmd+Option+F` / `Ctrl+H` 快捷键继续保留。
- 增加替换入口可见性、展开/收起状态和快捷键状态同步回归测试。

## 0.2.35 — 2026-08-15

- 修复 macOS Cocoa 原生样式下终端会话标签栏高度塌缩，已有标签内容无法显示、顶部只剩空白工具条的问题。
- 为会话标签栏设置稳定的 32px 布局高度，单标签、多标签及连接状态图标均保持可见。
- 增加 Cocoa 原生平台的标签可见性、控件高度、标签宽度、当前项和标题回归测试。

## 0.2.34 — 2026-08-15

- 远端文件编辑器新增紧凑型查找/替换面板，不操作时保持隐藏，不占用文件内容区域。
- 支持 `Cmd/Ctrl+F` 查找、macOS `Cmd+Option+F` 或 `Ctrl+H` 展开替换、Enter/Shift+Enter 循环定位和 Esc 收起。
- 新增匹配位置/总数、区分大小写、替换当前项和全部替换；替换操作进入编辑器撤销栈并同步标记文件未保存状态。
- 增加查找、循环定位、替换和脏状态回归测试。

## 0.2.33 — 2026-08-15

- 修复编辑主机 IP/端口后仍携带旧地址指纹，导致新服务器被“指纹不匹配”阻断的问题。
- 编辑窗口检测到地址或端口变更时自动清除原目标指纹；新地址首次连接会按安全流程重新确认，旧地址指纹记录不受影响。
- 终端标签新增连接状态图标：灰色空心点表示未连接，蓝色进度环表示连接中，绿色实心点表示连接成功；悬停提示展示状态、地址和底层连接信息。
- 终端标签右键菜单新增“连接”和“断开连接”，根据当前标签状态自动启用可执行的操作。
- 增加端点变更指纹清理、标签状态及右键连接/断开的回归测试。

## 0.2.32 — 2026-08-15

- 左侧第一栏收敛为纯主机入口，移除每台主机的在线/离线圆点与状态提示。
- 单击主机只保留列表选中项，不再激活或切换终端标签；终端、监控和 SFTP 联动统一由上方终端标签控制。
- 双击主机建立连接后自动隐藏第一栏，为终端和文件区释放宽度；可从 macOS 标题栏图标随时恢复。
- 第一栏编辑或删除其他主机时不再改变、断开当前终端快照，并修复连续删除主机后删除状态未释放的问题。
- 更新主机列表和主窗口回归测试，覆盖无状态标记、单击不切标签与双击自动收起。

## 0.2.31 — 2026-08-15

- 主机配置修改改为会话快照模型：已连接终端继续使用建立 SSH 时的原主机名、IP、端口和凭据。
- 保存主机修改不再断开、清屏或自动重连旧会话；下一次双击或右键连接时创建使用新配置的独立标签。
- 同一主机的新旧配置标签可并存，后续连接优先激活最新配置会话，避免重复建立 SSH。
- 增加在线编辑主机的回归测试，验证旧通道不重连、旧标签不改写以及新连接使用新 IP/端口。

## 0.2.30 — 2026-08-15

- 启动时固定保持未连接状态，不再恢复上次打开但未连接的终端标签；单击左侧主机也不会创建空标签。
- 当前没有 SSH 会话时隐藏整个标签栏与空终端内容，右上区域改为“最近登录”页面。
- SQLite 新增成功登录历史表，只在 SSH 认证成功后记录真实登录时间，并按登录时间倒序展示主机、地址、用户和时间。
- 最近登录记录支持双击重新连接；最后一个终端标签关闭后自动返回记录页。
- 增加旧标签状态不恢复、登录历史倒序、服务器删除级联清理及空闲/会话页面切换回归测试。

## 0.2.29 — 2026-08-15

- 修正 v0.2.28 的顶部控制区仍以独立 Qt 工具栏占据一行的问题。
- macOS 改用 Cocoa `NSTitlebarAccessoryViewController`，将主机列表与实时监控栏开关直接嵌入红黄绿系统按钮右侧。
- Cocoa 环境不再创建 `QToolBar`，应用内容紧贴系统标题栏，进一步释放顶部可视高度。
- 非 Cocoa 和无界面测试环境保留兼容工具栏，两个开关的行为与状态提示保持一致。

## 0.2.28 — 2026-08-15

- 删除整条深蓝色自定义顶栏及其中的品牌块、全局搜索占位、连接服务和用户信息，释放 56px 工作区高度。
- 主机列表开关移动到窗口顶部工具栏；macOS 下启用标题栏与工具栏融合。
- 顶部新增实时监控栏开关，可独立隐藏或恢复左侧第二栏，终端与文件区自动填充释放出的宽度。
- 为两个开关提供不同的栏位图标、展开/收起状态、悬停说明和无障碍名称，并增加双栏切换回归测试。

## 0.2.27 — 2026-08-14

- 文件管理的返回、上级、路径输入和刷新控件整体上移到标题工具栏，删除原有的独立路径栏。
- 移除标题栏中的“上传”“下载”和“+目录”按钮，减少常驻按钮和误操作；上传继续支持 Finder 拖拽，下载保留在右键菜单。
- 文件列表右键菜单新增“新建目录”，并与“新建文件”、下载、重命名和删除集中在同一操作菜单中。
- 增加单行工具栏布局和菜单入口回归测试，确保旧按钮不再生成。

## 0.2.26 — 2026-08-14

- 移除文件管理区底部常驻的传输队列和状态栏，释放更多目录浏览高度。
- 当前目录/传输状态合并到文件管理顶部工具栏，并在上传、下载等操作时原位更新。
- 工具栏新增传输队列图标；点击后以下拉面板展示任务详情、限速、展开/收起、取消与失败重试。
- 有正在执行或等待的传输任务时队列图标高亮，悬停提示同步展示活动状态和任务总数。

## 0.2.25 — 2026-08-14

- 修复旧版 SFTP v3 服务端在覆盖保存已有文件时返回 `SFTP 4`，导致临时文件无法提交的问题。
- 保存时优先使用 OpenSSH `posix-rename` 原子替换；不支持扩展时再尝试标准覆盖重命名。
- 对既不支持 POSIX rename、也拒绝覆盖标志的旧服务器增加备份提交回退，提交失败时自动恢复原文件。
- 上传文件复用同一套兼容提交逻辑，避免覆盖远端同名文件时出现相同错误。

## 0.2.24 — 2026-08-14

- 修复 macOS 下标准保存快捷键与额外 `Ctrl+S` 映射成同一按键序列，产生快捷键歧义而无法保存的问题。
- 编辑器改为直接处理按键事件，macOS 的 `⌘S` 和物理 `Ctrl+S` 均可保存，`⌘Z/Ctrl+Z`、`⌘W/Ctrl+W` 同样兼容。
- 回归测试真实发送 `⌘S` 与 `Ctrl+S`，分别保存后通过 SFTP 重新读取内容确认写入成功。

## 0.2.23 — 2026-08-14

- 远端文件编辑器改为上方文件标签、下方代码编辑区的多标签窗口；同一服务器打开多个文件时复用窗口。
- 文件标签显示“服务器名称 · 文件名”，未保存的标签使用红色圆点提示，保存成功后自动消失。
- 移除编辑器中的“关闭”和“保存”按钮，改用 `Cmd/Ctrl+S` 保存、`Cmd/Ctrl+Z` 撤销、`Cmd/Ctrl+W` 关闭当前标签。
- 编辑区新增行号、当前行高亮、等宽字体与 4 空格制表位；`Ctrl+/` 可对当前行或选中多行切换 `#` 注释。
- 关闭文件标签或整个编辑窗口时会检查所有未保存内容，并提供保存、放弃或取消关闭选项。

## 0.2.22 — 2026-08-14

- 左侧主机项改为“名称 / IP”单行布局，在线状态圆点固定显示在最右侧，并缩短列表项高度。
- 终端标签新增右键菜单，支持复制会话、关闭当前、关闭其他和关闭全部。
- 移除标签栏右侧独立“复制会话”按钮，会话操作统一收拢到所点标签的右键菜单；清屏按钮继续保留。
- 增加标签右键菜单的复制与三种关闭方式回归测试，校验“关闭其他”会正确保留右键所点会话。

## 0.2.21 — 2026-08-14

- 双击 SFTP 普通文件会打开独立的非模态编辑窗口，读取和保存复用当前终端的 SSH 会话。
- 编辑器支持 `Cmd/Ctrl+S`、未保存关闭确认、二进制内容拦截和 4 MiB 大小限制；保存使用远端临时文件原子替换并保留原文件权限。
- 文件区右键菜单和“⋯”菜单新增“新建文件”，创建空文件后自动刷新当前目录，同名目标存在时拒绝覆盖。
- 增加远端文件创建、读取、覆盖保护、编辑保存和目录刷新回归测试。

## 0.2.20 — 2026-08-14

- 左侧主机栏移除“主机 + 数量”标题行，新增按钮移到搜索框右侧并与其同行。
- 主机列表改为“名称 / 服务器地址”两行紧凑布局，右上角用绿色圆点表示在线，灰色空心点表示离线。
- 关闭某主机的最后一个终端标签后，左侧该主机立即更新为离线；若还有同主机在线标签则保持在线。
- SSH 终端标签文字精简为主机名称，不再显示 `root@`等用户名前缀。

## 0.2.19 — 2026-08-14

- 监控与 SFTP 改为直接复用每个终端标签已建立的 SSH 会话，移除切换主机时断开、重连辅助会话的机制。
- 多个终端标签保持各自在线，标签或左侧主机切换只切换监控与文件界面的展示对象，不再重新握手和认证。
- 每个 SSH 会话保留独立的 SFTP 界面状态，切换返回后恢复原来的目录、目录树和文件列表。
- 回归测试新增“双主机在线往返切换时零重连”与 SFTP 目录保留校验。

## 0.2.18 — 2026-08-14

- 修复从左侧切换到已在线终端时，只更新选中主机却未重建监控/SFTP 辅助 SSH 会话的问题。
- 终端认证成功后会再次校验当前主机的辅助会话，避免终端在线而监控显示“采样失败”、SFTP 显示“SSH 会话未连接”。
- 增加两台在线主机从左侧往返切换的回归测试，同时校验监控采样与远程文件列表恢复。

## 0.2.17 — 2026-08-14

- 切换 SSH 终端标签时同步选中左侧对应主机，并刷新主机地址、在线状态、监控历史与 SFTP 文件区。
- 切换到已在线终端时自动建立对应主机的监控/SFTP辅助会话；未连接的恢复标签仍保持手动连接策略。
- 左侧主机选择与终端标签切换改为双向联动，避免终端和监控/文件区显示不同服务器。

## 0.2.16 — 2026-08-14

- 密码与私钥口令框增加英文半角输入提示，关闭中文联想、自动大写和预测输入。
- 自动将中文句号 `。`、全角标点及全角字母数字转换为对应英文半角字符。
- 无法转换的中文/非 ASCII 字符会被阻止，并在密码框下方提醒切换英文输入法和显示核对。

## 0.2.15 — 2026-08-14

- 编辑主机时明确说明空密码框会沿用 Keychain 已保存密码，连接测试状态同步显示本次凭据来源。
- 密码框新增显示/隐藏操作，便于核对包含中文或特殊字符的当前输入。
- 已保存密码认证失败时给出重新输入或切换私钥认证的针对性提示，减少将旧凭据误判为 SSH 会话冲突。

## 0.2.14 — 2026-08-14

- 密码认证增加 `keyboard-interactive` 自动协商与 `password` 回退，兼容由 PAM/面板配置为键盘交互密码的 SSH 服务端。
- 键盘交互凭据采用线程局部上下文，多 SSH 会话并发认证时不会互相覆盖。
- 认证失败信息新增服务端允许方式与客户端已尝试方式，便于区分凭据错误和认证方式不匹配。

## 0.2.13 — 2026-08-14

- 终端“清屏”改为标准 `Ctrl+L` 语义：清除本地滚屏历史后通知远端 Shell 重绘，保留一行可继续输入的提示符。
- 演示 SSH 会话同步支持 `Ctrl+L` 屏幕重绘，与真实 Bash 会话表现一致。

## 0.2.12 — 2026-08-14

- SFTP 文件管理改为双栏布局：左侧目录树按节点逐层异步加载，右侧保留当前目录的完整文件明细。
- 目录树与路径栏、返回/上级、终端 `cd` 命令联动，并自动展开和选中当前路径。
- 右侧文件列表支持 Shift/Cmd 多选，右键可下载所选文件；多文件下载时统一选择本地目标目录。
- 支持从 Finder 拖拽一个或多个本地文件到右侧区域上传，并可将文件直接投放到某个远程目录。

## 0.2.11 — 2026-08-14

- 删除终端上方的工作区说明栏和终端内部重复标题栏，“清屏”移到“复制会话”右侧。
- 监控栏主机标识精简为 `IP:端口`，新增一键复制 IP 图标。
- 在线状态改为聚合监控/SFTP 和多终端实际连接结果，修复 SSH 已通但仍显示离线的问题。
- 双击连接后终端显示连接中 Loading 提示板，并随握手、指纹校验和认证阶段更新说明。

## 0.2.10 — 2026-08-14

- 移除工作区右上角的刷新、测试连接、编辑、删除和新建会话按钮，避免对当前主机的误操作。
- 新增/编辑 SSH 主机窗口新增“连接测试”，使用当前未保存的表单配置验证 TCP、SSH 握手、主机指纹和认证。
- 编辑时密码或私钥口令留空，连接测试会安全沿用系统凭据库中的原凭据；新输入值不会被旧凭据覆盖。

## 0.2.9 — 2026-08-14

- 主界面改为三块同时可见的运维布局：左侧纵向监控栏、右上 SSH 终端、右下文件管理。
- 移除监控/终端/文件管理之间的主页面切换，连接后可同时观察指标、操作终端和浏览文件。
- 新增横向与纵向可拖动分隔条；监控趋势改为按需展开，在紧凑侧栏中保留历史趋势、阈值和告警能力。

## 0.2.8 — 2026-08-14

- 顶部品牌名称右侧新增侧栏图标按钮，可收起和恢复左侧主机列表。
- 侧栏隐藏后主工作区自动扩展；按钮图标、悬浮提示和无障碍名称会随状态切换。

## 0.2.7 — 2026-08-14

- 主机选择与连接解耦：启动和单击左侧主机不再自动连接，双击或右键“连接”才启动 SSH、监控和 SFTP。
- 左侧主机增加右键菜单，支持连接、编辑、复制主机配置、复制连接地址和删除。
- 终端提交 `cd` 命令后同步右侧 SFTP 目录，支持绝对路径、相对路径、`..`、`~`、`$HOME` 及 Tab 补全后的命令识别。
- 恢复的终端标签默认处于待连接状态，独立“测试连接”成功后不再隐式替换现有终端连接。

## 0.2.2 — 2026-08-13

- 修复测试连接成功或修改凭据后，已打开终端标签仍保留旧认证失败状态的问题。
- SFTP 初始目录改为通过 `realpath(".")` 自动定位远端用户主目录，不再固定假设 `/var/www/app` 存在。
- 远端主目录无法解析时自动回退到根目录，并展示明确状态。

## 0.2.1 — 2026-08-13

- 修复 Qt TCP 缓冲区提前读取服务端 SSH banner，导致 libssh2 握手报 `Failed sending banner (-43)` 的问题。
- SSH 传输层改为 libssh2 独占的原生 IPv4/IPv6 socket，补充跨平台连接超时和 socket 错误信息。
- 增加可选真实服务器握手回归测试；不会提交密码或执行远端命令。

## 0.2.0 — 2026-08-13

- SSH 终端升级为真实多标签、独立连接、会话复制和启动恢复。
- 监控历史落盘保存 7 天，支持每台服务器独立阈值与告警事件记录。
- 传输队列支持持久化、异常退出恢复、失败重试、断点续传和会话级限速。
- 凭据存储覆盖 macOS Keychain、Windows Credential Manager 与 Linux Secret Service。
- 新增滚动日志、敏感字段脱敏、应用图标和跨平台发布脚本。

## 0.1.0

- 完成 SSH 会话、VT/ANSI 终端、Linux 指标采集与 SFTP 文件管理基础能力。
