# 为玄壳 NoxShell 贡献

感谢参与玄壳 NoxShell 的改进。提交贡献即表示你同意将原创贡献按项目根目录的 Apache License 2.0 授权。

## 报告问题

- 先搜索现有 Issue，避免重复报告。
- 说明操作系统、NoxShell 版本、复现步骤、期望结果与实际结果。
- 可提供已脱敏的日志和截图，但不要提交服务器密码、私钥、令牌、真实业务数据或其他敏感信息。

## 提交代码

1. 从 `main` 创建独立分支。
2. 保持改动聚焦，并为行为变化补充或更新测试。
3. 在提交前完成本地验证：

```bash
./script/qt-ui-start.sh --build-only
ctest --test-dir build --output-on-failure
```

4. 提交 Pull Request，说明修改原因、实现方式和验证结果；界面改动请附截图。

## 开发约定

- 使用 C++20 和 Qt 6，遵循现有目录结构与命名风格。
- 不要把密码、私钥、测试服务器凭据或本机数据库提交到仓库。
- 新增第三方依赖时，请同时说明用途、许可证和二进制分发影响，并更新 `THIRD_PARTY_NOTICES.md`。
- 涉及 SSH、SFTP、凭据保存和远程文件操作的改动，应优先保证失败路径安全且错误信息明确。
